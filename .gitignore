node_modules/
.env
artifacts/
cache/
coverage/
coverage.json
typechain/
typechain-types/
*.log
data/

# Supabase CLI local state (contains the linked project ref / pooler URL)
supabase/.temp/

# Personal working notes — useful locally, not for a public repo
SUBMISSION.md
DEMO.md
.env.example
